




export function increaseCartQuantity(_id: number) {
  // setCartItems((currItem) => {
  //   if (currItem.find((item) => item._id === _id) == null) {
  //     return [...currItem, { _id, quantity: 1 }];
  //   } else {
  //     return currItem.map((item) => {
  //       if (item._id === _id) {
  //         return { ...item, quantity: item.quantity + 1 };
  //       } else {
  //         return item;
  //       }
  //     });
  //   }
  // });
  console.log(_id)
}
